<h1>Listing tasks</h1>

<!-- 
  Note that we don't have to use paginated_section(@tasks) - 
  it infers it from the controller.  If the instance variable
  does not match the controller name, we would need to explicitly
  add it.
-->
<%- paginated_section :renderer => 'TaskRenderer' do -%>
<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% for task in @tasks %>
  <tr>
    <td><%=h task.name %></td>
    <td><%= link_to 'Show', task_path(:id => task, :page => page_param) %></td>
    <td><%= link_to 'Edit', edit_task_path(:id => task, :page => page_param) %></td>
    <td><%= link_to 'Destroy', task_path(:id => task, :page => page_param), :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>
<%- end -%>

<p>
<%= link_to 'New task', new_task_path(:page => page_param) %>
</p>